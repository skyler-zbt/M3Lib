// m3.detail — re-exports all detail submodules (qualifier, concepts, storage).
//
// m3.detail —— 重导出所有 detail 子模块（qualifier、concepts、storage）。
export module m3.detail;

export import m3.detail.qualifier;
export import m3.detail.concepts;
export import m3.detail.storage;
