// m3.detail.storage — re-exports storage submodules.
// Currently: vector_storage (aligned std::array wrapper).
//
// m3.detail.storage —— 重导出存储子模块。
// 当前包含：vector_storage（对齐的 std::array 包装）。
export module m3.detail.storage;

export import m3.detail.storage.vector_storage;
